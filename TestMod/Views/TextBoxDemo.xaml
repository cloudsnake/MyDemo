<UserControl x:Class="TestMod.Views.TextBoxDemo"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"></ColumnDefinition>
            <ColumnDefinition Width="50*"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="26"></RowDefinition>
            <RowDefinition Height="26"></RowDefinition>
        </Grid.RowDefinitions>

        <TextBox x:Name="text1" Grid.Row="0" Grid.Column="0" Width="150" Text="{Binding FenShu,Mode=TwoWay}"
                 TextInput="Text1_OnTextInput"
                 TextChanged="Text1_OnTextChanged"
                 >
        </TextBox>
        <TextBox x:Name="text2" Grid.Row="0" Grid.Column="1" Text="{Binding Fenshu10,Mode=TwoWay}"></TextBox>
    </Grid>
</UserControl>
